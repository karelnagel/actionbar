---
import { Locale, LocaleInfo } from "@actionbar/i18n";
import { getLocaleUrl } from "@actionbar/shared/helpers";
import { getPathname } from "@actionbar/shared/helpers";

const pathname = getPathname(Astro.url.pathname);
---

<div>
  {
    Locale.options.map((locale) => (
      <a class="button" href={getLocaleUrl(locale, pathname)}>
        {LocaleInfo[locale].title}
      </a>
    ))
  }
</div>
